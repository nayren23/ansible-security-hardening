---
- name: Install Apache and PHP packages
  ansible.builtin.apt:
    name:
      - apache2
      - php
      - php-mysql
      - libapache2-mod-php
    state: present
    update_cache: true

- name: Enable and start Apache
  ansible.builtin.service:
    name: apache2
    state: started
    enabled: true
